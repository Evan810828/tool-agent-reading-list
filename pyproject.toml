[project]
name = "llm-watermark-hub"
version = "0.1.0"
description = "Static GitHub Pages site and updater scripts for LLM watermark research."
requires-python = ">=3.9"
dependencies = [
    "pyyaml>=6.0.2",
    "requests>=2.32.0",
]

[tool.uv]
package = false
